info:
  name: Auth Client Credentials
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/realms/dev/protocol/openid-connect/token
  body:
    type: form-urlencoded
    data:
      - name: grant_type
        value: client_credentials
      - name: client_id
        value: newClient
      - name: client_secret
        value: newClientSecret
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        var jsonData = res.getBody();
        if (jsonData["access_token"] != null) {
            bru.setEnvVar("auth_token", jsonData["access_token"]);
        }
        if (jsonData["token_type"] != null) {
            bru.setEnvVar("token_type", jsonData["token_type"]);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
